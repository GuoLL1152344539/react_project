import {SAVE_SUBJECT_INFO} from './constants'


const initSubjectInfo = {total:0, item:[]}
export default function (preState, action) {
  const {type, data} = action
  switch (type) {
    case SAVE_SUBJECT_INFO:
      
      break;
  
    default:
      break;
  }
}